apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-task.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "test-task.name" . }}
    helm.sh/chart: {{ include "test-task.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "test-task.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
